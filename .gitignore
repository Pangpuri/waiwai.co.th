# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# local tooling / agent settings — มี API key อยู่ข้างใน ห้าม commit
# (ใส่ทั้งแบบมีและไม่มี slash เผื่อวันหนึ่งมันกลายเป็นไฟล์/symlink ที่ git มองไม่เป็นโฟลเดอร์)
.deepcode/
.deepcode

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# ─────────────────────────────────────────────────────────────────────────────
# ข้อมูลต้นทางจากบริษัท — ใช้ทำเว็บเท่านั้น ห้าม commit / ห้ามเผยแพร่
# (สำเนาที่ใช้จริงถูกคัดลอกเข้า public/ แล้ว จึง build ได้โดยไม่ต้องมีโฟลเดอร์เหล่านี้)
# ─────────────────────────────────────────────────────────────────────────────
/cer/
/provider/
/products/
/contact/
/company.txt
/Work with Wai Wai.txt

# ─────────────────────────────────────────────────────────────────────────────
# เอกสารภายใน (บรีฟ / roadmap / กฎ) — เก็บไว้ในเครื่อง ไม่ขึ้น repo
# ⚠️ _CODINGRULES.md_Next.js.md มีข้อมูลของโปรเจกต์อื่น (asset-lending SaaS) ปนอยู่
# ─────────────────────────────────────────────────────────────────────────────
/AGENTS.md
/CLAUDE.md
/PRODUCT_ROADMAP.md
/CODING_RULES.en.md
/_CODINGRULES.md_Next.js.md
